# Copy from ../repnet/ecg/batch_builder.py and clean up
#
# Cleanup tasks:
# - Remove unused imports (nn, if not used directly)
# - Remove unused variable PHI in build_class_guaranteed_batches
# - Run ruff --fix for auto-fixable warnings
# - Clean up semicolons
