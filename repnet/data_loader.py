# Copy from ../repnet/ecg/data_loader.py and clean up
#
# Cleanup tasks:
# - Update imports to use repnet.models instead of models_ecg
# - Remove unused import os (if unused after cleanup)
# - Consider making data path configurable via argument or environment variable
